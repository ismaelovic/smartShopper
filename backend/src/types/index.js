// backend/src/types/index.js
// This file would contain JSDoc comments for defining types if not using TypeScript.
// For example:
/**
* @typedef {object} StoreInfo
* @property {string} brand
* @property {string} address
* @property {string} id
*/

/**
* @typedef {object} ClearanceItem
* @property {string} productDescription
* @property {string} offerHeading
* @property {number} newPrice
* @property {number} originalPrice
* @property {number} discount
* @property {string} imageUrl
* @property {string} validUntil
* @property {StoreInfo} store
*/

// No actual code to export for now, just type definitions.